# SDK Helpers

Minimal OpenAI-compatible helpers for local integration:

- Python: `sdk/python/amaryllis_openai_compat.py`
- JavaScript: `sdk/javascript/amaryllis_openai_compat.mjs`

Both wrappers target:

- `POST /v1/chat/completions`

and keep dependencies minimal for quick prototypes.
